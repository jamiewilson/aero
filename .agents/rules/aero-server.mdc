---
description: Nitro API and server conventions
globs: examples/**/server/**/*.ts,examples/**/backend/**/*.ts,packages/create/templates/**/server/**/*.ts
alwaysApply: false
---

# Aero Server (Nitro)

Document discoveries from code exploration in [_reference/DISCOVERY.md](../../_reference/DISCOVERY.md).

## Layout

API handlers and Nitro routes live in the **app’s** server directory—commonly `server/` (e.g. `packages/create/templates/fullstack/server/`) or `backend/` when configured (e.g. `examples/kitchen-sink/backend/`). Nitro is configured at the **app root** (`nitro.config.ts`) alongside `aero.config.ts` / Vite config.

## API handlers

- Use Nitro/H3: `defineHandler`, `readBody`, etc. from `nitro/h3`.
- File naming follows Nitro conventions (e.g. `submit.post.ts` → `POST /api/submit`).
- Types: use project types or types exported from `@aero-js/core` where applicable.

## Example

```ts
import { defineHandler, readBody } from 'nitro/h3'

export default defineHandler(async event => {
	const body = await readBody(event)
	return { ok: true, data: body }
})
```

See [Nitro](../../docs/_archive/server/nitro.mdx) and [Deploy with Nitro](../../docs/getting-started/deployment.mdx).
